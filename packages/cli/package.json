{
  "name": "ts-note",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sinapis/ts-note"
  },
  "dependencies": {
    "@ts-note/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@ts-note/local-api": "^1.0.0",
    "commander": "^9.3.0",
    "@types/node": "^18.0.0",
    "esbuild": "^0.14.47",
    "typescript": "^4.7.3"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index.js"
}
